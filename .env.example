# Configurazione locale (copiare in .env)
DB_BACKEND=local
# DB_PATH=./DATABASE.xlsx  # opzionale, default: ./DATABASE.xlsx

# Configurazione Supabase (per produzione)
# SUPABASE_URL=https://tuo-progetto.supabase.co
# SUPABASE_KEY=eyJ...  # anon key

# Flask
FLASK_ENV=production
PORT=5000
